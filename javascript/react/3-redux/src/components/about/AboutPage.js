import React from 'react'

const AboutPage = (props) => (
  <div className="page">
    <h1>About</h1>
    <p>This application uses React, Redux, React Router, and a number of other great technologies.</p>
  </div>
)

export default AboutPage
